import React from 'react';
import LoginForm from "../authorization/LoginForm";

const Login = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default Login;